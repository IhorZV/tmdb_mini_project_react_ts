const baseURL = 'https://api.themoviedb.org/3';

const movies = "/discover/movie";
const movie = "/movie";
const allGenres = "/genre/movie/list";

const urls = {
    movies,
    movie,
    allGenres
}

export {
    baseURL,
    urls
}